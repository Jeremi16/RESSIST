{
  "name": "ressist-monorepo",
  "private": true,
  "scripts": {
    "dev": "bun run dev:frontend & bun run dev:backend & wait",
    "dev:frontend": "bun --cwd frontend run dev",
    "dev:backend": "cd backend && go run ./cmd/api",
    "build": "bun --cwd frontend run build && cd backend && go build ./..."
  }
}
