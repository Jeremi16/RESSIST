# Release signing (gitignored — NEVER commit).
# Copy to mobile-kmp/keystore.properties and fill passwords.
# storeFile is relative to mobile-kmp/.
storeFile=ressist-release.jks
storePassword=change-me
keyAlias=ressist
keyPassword=change-me
